import styled from 'styled-components';

const CardsWrapper = styled.article`
  width: 100px;
  height: 120px;

  picture image {
    width: auto;
    height: auto;
  }

  h3 {
    text-align: center;
  }
`;

export default CardsWrapper;
